# Локальный профиль — переопределяет значения для разработки
# Не коммитить реальные ключи!
app:
  gitlab:
    url: http://10.1.5.6/
    token: ${GITLAB_TOKEN:Wg2zKVFW6ZogLt1D3dHQ}
    webhook-token: ${GITLAB_WEBHOOK_TOKEN:changeme}
  graph-service:
    url: http://10.1.5.97:8082/api/review/

spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:dummy}
      base-url: ${OPENAI_BASE_URL:https://api.openai.com}
